// Re-export types from shared for frontend consumption
export { Presence, PresenceUpdate } from "../../shared/types";
export type { User, Workspace, Note, Folder, Template } from "../../shared/types";
